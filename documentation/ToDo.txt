- Write again to 2BM, NSLS, SLS, LBL, Jeep, Australia if I don't hear back.

- Make C simple application that reconstructs a .raw dataset with chunking

- Make debug file be a TomoParams parameter

- Make tomoRecon object able to stay around to process more data with the same parameters
  - abort() aborts the current job
  - Constructor only takes tomoParms
  - compute() takes input and output pointers
  
- Speed up preprocessing using either threads or GPU

- Do documentation of IDL functions

- Do documentation of C++ functions

- Write SPIE paper

- Write SPIE talk


