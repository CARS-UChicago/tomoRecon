- Fix problem that running again on Windows leads to immediate exit from C

- Add ring artifact reduction to sinogram

- Allow different center for each slice.  Only re-do if the center has changed.

- Get IDL tomo_recon_netcdf code working to do the reconstruction in "chunks", 
  overlapping file I/O and reconstruction
  
- Put code in SVN repository

- Write to Swiss Light Source, Australian Synchrotron, LBL, 2BM to get their
  benchmarks for typical sizes:
  - Break out time for reading and writing compared to reconstruction
  - Typical sizes, range that they use.
