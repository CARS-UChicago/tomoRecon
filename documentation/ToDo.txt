- Make C simple application that reconstructs a .raw dataset with chunking

- See if chunk tests produce the same data as non-chunk tests

- Optimum center is not consistent depending on initial guess

- See if centers from tomoRecon agree with old software.  If not, why not?

- Speed up preprocessing using either threads or GPU

- Test and report performance problems with writeu on Linux and Windows

- Do documentation of IDL functions
